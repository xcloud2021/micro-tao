spring:
  cloud:
    config:
      name: config-cart
      profile: dev
      uri: http://localhost:3001
  rabbitmq:
    host: 115.159.124.112
    port: 5672
    username: rabbitmquser
    password: rabbitmqpass

#  暴露触发消息总线的地址
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
